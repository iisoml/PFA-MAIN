# ============================================================
# Production requirements — serving only (FastAPI + inference)
# ============================================================
# Key constraint: tensorflow==2.17.0 requires numpy<2.0.0
# → numpy is pinned to 1.26.4 (latest 1.x, satisfies all packages)

# --- Web framework ---
fastapi==0.115.2
uvicorn==0.30.5
starlette==0.40.0
pydantic==2.8.2
pydantic_core==2.20.1

# --- UI ---
gradio==5.9.1

# --- ML / inference ---
# numpy MUST stay <2.0.0 — tensorflow 2.17.0 has a hard upper bound
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
scipy==1.14.1            # 1.16.x requires numpy>=2.x internally; 1.14.1 is the last numpy-1.x-safe release
xgboost==2.1.3
tensorflow==2.17.0
joblib==1.4.2

# --- Experiment tracking ---
mlflow==2.16.2

# --- Utilities ---
requests==2.32.4
python-dotenv==1.0.1
PyYAML==6.0.2
tqdm==4.67.1
pyarrow==17.0.0
cloudpickle==3.1.0
protobuf==4.25.8
pytz==2024.2
tzdata==2024.2
typing_extensions==4.12.2
anyio==4.10.0
sniffio==1.3.1
h11==0.16.0
click==8.1.7